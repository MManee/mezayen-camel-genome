# Recovered NextDenovo configuration parameters
#
# These parameters were recovered from retained workflow records.
# This file does not claim to reconstruct every parameter from the
# original NextDenovo configuration.

[General]
read_type = clr
input_fofn = ./input.fofn
genome_size = 3g
read_cutoff = 1k
pa_correction = 5
